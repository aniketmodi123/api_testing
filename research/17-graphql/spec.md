# Spec — GraphQL

STATUS: stable (existing)
LAST_CHANGED: 2026-06-16

## Current State
GraphQL works via JSON body mode. Introspection provides schema. No new backend work needed.

## Pending (FE only, P3)
- GraphQL variables tab (separate UI field for `variables` JSON alongside `query`)
- Operation name picker from introspection schema
