const Logo = () => {
  const styles = {
    logo: {
      fontWeight: 700,
      fontSize: '1.2rem',
      color: 'var(--text-color)',
      margin: 0,
    },
  };

  return <h1 style={styles.logo}>API Testing</h1>;
};

export default Logo;
