// Spacing token names. Values owned by presets/spacing.js (density presets).
export const SPACING_TOKEN_KEYS = [
  '--space-1', '--space-2', '--space-3', '--space-4', '--space-5', '--space-6',
];

// No static spacing tokens — all values owned by density presets.
export const STATIC_SPACING_TOKENS = {};
